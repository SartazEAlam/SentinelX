import { Bell } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

export default function Alerts() {
  return <PlaceholderPage title="Security Alerts" phase={4} icon={Bell} />;
}
