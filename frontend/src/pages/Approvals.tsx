import { CheckCircle2 } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

export default function Approvals() {
  return <PlaceholderPage title="Approval Requests" phase={5} icon={CheckCircle2} />;
}
