import { ScrollText } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

export default function Policies() {
  return <PlaceholderPage title="Security Policies" phase={4} icon={ScrollText} />;
}
