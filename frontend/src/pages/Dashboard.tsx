import { LayoutDashboard } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

export default function Dashboard() {
  return <PlaceholderPage title="Dashboard" phase={6} icon={LayoutDashboard} />;
}
