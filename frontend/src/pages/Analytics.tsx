import { TrendingUp } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

export default function Analytics() {
  return <PlaceholderPage title="Security Analytics" phase={8} icon={TrendingUp} />;
}
