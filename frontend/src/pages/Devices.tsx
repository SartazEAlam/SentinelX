import { Laptop } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

export default function Devices() {
  return <PlaceholderPage title="Managed Devices" phase={2} icon={Laptop} />;
}
